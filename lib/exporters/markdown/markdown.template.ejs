# <%= header.kdbxFile %>
- **created:** <%= new Date().toISOString() %>
<% groups.forEach(group => { %>
## <%= group.name %><% if (group.tags && group.tags.length) { %> [<%= group.tags.join(", ") %>]<% } %>
- **last modified:** <%= new Date(group.times.lastModTime).toISOString() %>
<% if (group.notes && group.notes.length) { %>```
<%= group.notes %>
```<% } %>
<% group.entries.forEach(entry => { %>
### <%= entry.fields.Title %><% if (entry.tags && entry.tags.length) { %> [_<%= entry.tags.join(", ") %>_]<% } %>
<% if (entry.fields.URL) { %>- **Url:** [<%= entry.fields.URL %>](<%= entry.fields.URL %>)<% } %>
- **Username:** <%= entry.fields.UserName %>
- **Password:** <%= entry.fields.Password %>
- **last modified:** <%= new Date(entry.times.lastModTime).toISOString() %>

<% if (entry.fields.Notes && entry.fields.Notes.length) { %>```
<%= entry.fields.Notes %>
```<% } %>
<% }) %><% }) %>
----------